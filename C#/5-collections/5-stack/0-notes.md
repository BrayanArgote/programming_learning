# Create
Stack<T> name = new Stack<T>();

# Add
stackName.Push(value);

# Get/Pop
stackName.Pop();    = return and delete the element
stackName.Peek();   = return the element but don't delete it

# Check if exists
stackName.Contains(value);

# Delete 
nameStack.Clear();

# Utils
stackName.Count;
stackName.TryPop(out variable);
stack.TryPeek(out variable);