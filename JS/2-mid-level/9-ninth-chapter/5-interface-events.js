error          // for images 

load           // when the site has loaded

beforeunload   // when the user is about to leave the site

unload         // whe the user leaves the site

resize         // when the window size changes

scroll         // when the user scrolls

select         // when to selects something in an input