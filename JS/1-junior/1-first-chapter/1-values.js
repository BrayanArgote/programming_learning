// initializanding values
let name = "Juan";
let age = 20;
alert(name +" " + age);

// null (the developer intetionally set the variable to null)
let n = null;
alert(n);

// undefined (the variable exists but has no value)
let u;
alert(u);

// NaN (the result is "Not a Number")
let num = 8;
let word = "Pablo";
alert(8 * word)